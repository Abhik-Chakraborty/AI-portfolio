# Local secrets (optional Spring config import — see backend/src/main/resources/application.properties)
backend/config/local.properties

# Gradle
backend/.gradle/
backend/build/

PROJECT_OVERVIEW.md

# Logs — any logs/ directory at any depth (e.g. backend/logs/) and all .log files
logs/
*.log

# Node dependencies — any node_modules/ at any depth (e.g. frontend/node_modules/)
node_modules/